*.js
!jest.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# CDK synthesized output directories (contain AWS account IDs)
cdk-deploy-out
cdk-pipeline-out

# TypeScript incremental compilation cache (DDR-047)
.tsbuildinfo

# CDK context (contains SSM lookup results; do not commit connection ARN)
cdk.context.json

# Local build artifacts for initial deployment
.build
